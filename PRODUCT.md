# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

个人视频创作者/剪辑师。日常需要从视频里挑素材帧，用于封面制作、分镜稿、素材库整理等场景。通常一次要处理多个视频文件，且不同视频可能需要不同的截取区间和抽帧密度。

## Product Purpose

一个跨平台（Windows/macOS）的批量视频抽帧桌面工具。基于本地 FFmpeg 处理，用户选定一批视频后，可对每个视频独立设置时间范围，并选择抽帧策略（按帧间隔 / 按秒间隔 / 关键帧 / 场景切换），批量导出为图片序列。

## Positioning

同类方案是直接敲 FFmpeg 命令行、用播放器截图、或上传到在线抽帧网站。本产品的差异点：
- **批量处理**：一次选多个视频，依次自动处理，不需要逐个敲命令。
- **每视频独立参数**：每个视频可单独设置时间范围（同一批次里，不同视频要求不同的截取区间是常态）。
- **本地处理**：视频不上传，处理速度和隐私都优于在线工具。
- **内置 FFmpeg**：开箱即用，用户不需要自己安装/配置命令行工具，比裸命令行工具更易上手。

## Operating Context

Electron 桌面应用，当前支持 Windows 和 macOS。内置 FFmpeg/FFprobe 二进制，无需用户额外安装依赖。四个核心页面：视频切帧（批处理主界面）、预览（图片序列播放器 + 视频时间范围选择器）、处理记录（历史日志）、设置（输出目录、窗口关闭行为）。

## Capabilities and Constraints

- 批量选择视频文件，并行读取每个视频的元信息（分辨率、帧率、时长）。
- 每个视频可独立设置起止时间（HH:MM:SS 格式文本输入）。
- 四种导出模式：按帧间隔、按秒间隔、关键帧提取、场景切换检测（可调阈值）。
- 输出格式 jpg/png/webp，质量滑块，尺寸缩放（百分比或固定宽高），JPEG 色度采样可选。
- 输出目录可按视频所在目录，也可指定全局自定义目录，或单次批处理临时覆盖。
- 提取过程有实时进度条（按已处理时长估算整体进度）和 FFmpeg 执行日志（指令、stderr、成功/失败）。
- 预览页：图片序列播放器（播放/暂停/逐帧/导出当前帧）；视频预览带双滑块时间范围选择器，可一键把选定范围带入切帧页。
- 处理记录页：保留最近 100 条批处理历史（成功/失败状态、帧数、输出目录、错误信息），支持清空。
- 技术栈：Electron + React 18 + React Router 6 + Vite；渲染层为纯前端页面，与 Electron 主进程通过 IPC 通信；FFmpeg 调用走 fluent-ffmpeg。

## Brand Commitments

无强制保留项。名称 "Video2Frame" 和当前深色配色方案均可自由调整；用户明确表示这次希望视觉方案完全重新设计，不受现有实现约束。

## Product Principles

1. **批处理优先**：界面要让"一次处理多个、每个又能单独调"这件事顺手，不能因为支持了灵活参数就牺牲批量操作的效率。
2. **本地工具的可信感**：用户在处理私人素材，界面要传达"这是运行在我电脑上的可靠工具"，不是营销产品或玩具应用。
3. **专业但不吓人**：目标用户是视频创作者而非工程师，参数需要专业（帧率、色度采样、场景阈值这些术语要保留），但呈现方式不能像命令行工具一样冷峻。
4. **过程可见**：批量处理耗时较长，进度、日志、结果反馈必须清晰及时，用户要随时知道"现在在处理哪个、处理到什么程度、出错了没有"。
